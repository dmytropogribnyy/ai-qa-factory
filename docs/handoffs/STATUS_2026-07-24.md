# AI QA Factory — Verified Current State (2026-07-24)

This is the concise current-state checkpoint for cold-start operators. Historical detail remains in `docs/handoffs/CURRENT.md`; when the two disagree, this checkpoint is newer.

## Canonical repository state

- Repository: `dmytropogribnyy/ai-qa-factory`
- Canonical `main`: `ba9e9db550c73dae425a2855daabec4dfe84737e`
- Latest merged product PR: #43 — runtime claims aligned with verified capabilities
- Open product PRs: none after closing stale draft PR #19 as superseded

## Recently completed slices

- #39 — persist `video_mode` across run storage and immutable resume signatures
- #40 — pin Ruff `0.15.22` after the `0.16.0` default-rule expansion broke the established lint contract
- #41 — bounded redacted evidence bundles: separate landing/verification screenshots, browser trace, evidence manifest, integrity hashes, exact-run Dashboard and Observer linkage
- #42 — select the newest persisted campaign evidence for a domain while preserving explicit exact-run pinning
- #43 — align active runtime/version/capability claims, fix default pytest collection, and polish the mobile Manual URL Scan layout

## Verified product capability

Scout currently supports bounded public-site QA with:

- static or Playwright Deep Capture modes;
- Adaptive/Deep within-site coverage profiles;
- exact-run and exact-prospect Results/Target evidence;
- screenshots, axe-core accessibility evidence, console/network/performance evidence;
- bounded redacted browser trace and evidence manifest with SHA-256 integrity metadata;
- optional qualified reproduction video only for a genuinely replayed safe interaction defect with verified cleanup;
- Manual URL Scan and curated XLSX/CSV import;
- History, Activity, evidence opening and safe artifact serving;
- loopback, Origin/Host/CSRF, URL-safety, path-confinement and cross-run/cross-prospect isolation.

## Honest limits

- Current automatic Scout execution is read-only navigation; policy ceilings are not evidence that an interaction ran.
- Safe multi-step reversible flows remain a separately reviewed future slice.
- No CAPTCHA solving or bypass.
- No autonomous outreach.
- No real purchase, booking, reservation, message or contact-form submission.
- Dashboard/read paths must remain free of paid LLM calls.

## CI and review truth

PR #43 exact head `4e4cd5afadec880b945b6d12dee140d2e29ece74` passed authoritative full CI run #168, including Linux full tests, Windows full tests, real Chromium/browser acceptance, provider contracts and relay smoke. Conditional path-filter jobs skipped as expected.

The recorded PR #43 review was explicitly a transparent self-audit, not an independent relay approval. Future safety-sensitive changes to browser actions, cleanup, redaction, artifact confinement or external boundaries require a genuinely independent reviewer channel.

## Stale branch handling

Draft PR #19 was closed without merge. It was 43 commits behind `main` and diverged across 25 files. Potentially useful concepts — fenced single-writer claims, token-gated heartbeat/resolve, liveness-aware recovery, product packets and bounded relaunch — remain available in the branch but must be re-audited and reintroduced only as fresh current-main bounded slices.

## Next bounded work

1. Run a fresh real operator smoke on current `main` and fix only confirmed defects.
2. Design safe typed multi-step reversible flow primitives as a separate PR series with deterministic fixtures, strict prohibited boundaries, cleanup verification and client-safe gating.
3. Reuse any valuable PR #19 collaboration ideas only after a fresh current-main reuse analysis.

Do not broaden these into autonomous external actions or outreach.