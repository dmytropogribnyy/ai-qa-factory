# Agent Handoff Report Template

**Version:** 5.2.0
**Updated:** 2026-05-25

Copy this template for the final response of every agent implementation or review phase.
Fill in every section. Do not omit sections — use "N/A" or "none" if a section is empty.

---

# Phase [X] Handoff Report

## Summary

One or two sentences: what was implemented or reviewed, and what is the current state.

---

## Changed Files

| File | Change type | Notes |
|---|---|---|
| `core/example.py` | modified | short description |
| `docs/EXAMPLE.md` | modified | updated doc |

---

## New Files

| File | Purpose |
|---|---|
| `core/new_module.py` | short description |
| `tests/test_new_module.py` | tests for new module |
| `docs/NEW_DOC.md` | new governance doc |

---

## Tests Run

```bash
python -m pytest -q
```

Result: `[N] passed in [X]s`

New tests added: [N] (describe what they cover)

---

## Docs Audit

```bash
python tools/docs_audit.py --no-write
```

Result: `[N] checks | 0 errors | 0 warnings | PASS` (or describe failures)

---

## Agent Readiness Audit

```bash
python tools/agent_readiness_audit.py --no-write
```

Result: `[N] checks | 0 errors | PASS` (or describe failures)

---

## Safety Boundary

| Check | Result |
|---|---|
| URL fetching performed | No |
| Browser execution performed | No |
| Credential use performed | No |
| External API calls performed | No |
| outputs/ staged | No |

---

## Secrets / Redaction

State whether any secrets were detected in inputs, and whether redaction was applied.

Example: "Input contained password= — redacted to [REDACTED_PASSWORD] before storage.
No raw secrets in any generated artifact."

Or: "No secrets detected in inputs."

---

## Generated Artifacts

List any runtime artifacts written to `outputs/`:

```
outputs/<project_id>/00_project/
    INPUT_MAP.json / .md
    WORK_REQUEST.json / .md
    ...
```

Or: "No artifacts generated (no-write mode)."

---

## Git Status Summary

```bash
git status
```

Paste or summarize the output. Confirm:
- [ ] No `.env` staged
- [ ] No `outputs/` staged
- [ ] No secrets staged
- [ ] No `node_modules/` or `.venv/` staged
- [ ] Only intended files are modified or untracked

---

## Intentionally Not Implemented

List what was explicitly out of scope for this phase:

- Phase 2C Strategy Planner — not started
- Playwright scaffold — Phase 3A scope
- URL fetching — blocked until Phase 4A approval
- (etc.)

---

## Blockers

Any issues that prevent proceeding to the next phase:

- [ ] none

Or describe specific blockers and what is needed to resolve them.

---

## Recommended Next Step

One sentence: what should the next phase or action be.

Example: "Phase 2C — QA Strategy Planner: consume `PROJECT_BLUEPRINT.json` and
produce `QA_STRATEGY.md` / `RISK_MATRIX.md` (planning-only, no execution)."

---

_Generated by [agent name] under [AGENT_CONTRACT.md](AGENT_CONTRACT.md) operating contract._
