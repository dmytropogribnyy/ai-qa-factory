"""Prospect QA Scout — Final Phase I pre-send pipeline (deep QA, evidence, findings, lifecycle).

The pre-send pipeline extends the Scout QA runtime with adaptive deep QA (real axe + an
honestly-named performance-observation layer + deep technical SEO + safe bounded business-flow
QA), an evidence center with finding normalization and a full finding lifecycle, persistent
company/site memory, rechecks, contact intelligence, audit offers, controlled disclosure,
outreach drafts, and human review queues. **Nothing is ever sent.**
"""
from __future__ import annotations

PIPELINE_VERSION = "1.9.0"
