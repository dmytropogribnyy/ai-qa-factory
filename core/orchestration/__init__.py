"""ARK orchestration layer — Phase 8.1 (planning-only).

This package composes existing intake engines and the Phase 8.0 schemas into a
deterministic, planning-only work pipeline. It performs NO LLM calls, NO network,
NO subprocess, NO browser, and NO MCP calls. Live MCP discovery is Phase 8.3.
"""
