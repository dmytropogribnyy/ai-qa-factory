[pytest]
testpaths = tests
markers =
    integration: needs a real network, DNS or browser. Unit tests must not; a suite that fails
        because a name did not resolve is reporting the environment, not the code.
